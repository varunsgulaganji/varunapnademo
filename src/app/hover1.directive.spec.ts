import { Hover1Directive } from './hover1.directive';

describe('Hover1Directive', () => {
  it('should create an instance', () => {
    const directive = new Hover1Directive();
    expect(directive).toBeTruthy();
  });
});
